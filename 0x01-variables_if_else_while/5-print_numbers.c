#include <stdio.h>

/**
 * main - Printss numberss from { 0 to 9}.
 *
 * Return: Alwayss 0 (Successs=1)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
