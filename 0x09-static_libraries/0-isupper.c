#include "main.h"

/**
 * _isupper - tests for capitalization
 * @c: character to be verified
 * Return: 1 0 if c is capitalised; otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
