#include "3-calc.h"

/**
 * op_add - the calculates the sum of two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: he is sum of a and b
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - the calculatesd the differenced of two integersd
 * @a: the first integer
 * @b: the second integer
 *
 * Return: that is difference of a and b
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - the calculates the producted of two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: product of a and b
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - the calculates the divisiond of two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: that result of the division of a and b
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - the calculates the remainder of the division of two integers
 * @a: he first integer
 * @b: he second integer
 *
 * Return: he remainder of a divided b
 */
int op_mod(int a, int b)
{
	return (a % b);
}
